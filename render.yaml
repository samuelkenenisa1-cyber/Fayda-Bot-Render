services:
  - type: web
    name: fayda-id-bot
    env: python
    pythonVersion: 3.11
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: python keep_alive.py
    envVars:
      - key: BOT_TOKEN
        sync: false
    plan: free
